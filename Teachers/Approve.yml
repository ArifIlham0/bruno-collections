info:
  name: Approve
  type: http
  seq: 3

http:
  method: POST
  url: "{{BASE_URL}}/teacher/approve-teacher/"
  body:
    type: json
    data: |-
      {
        "teacher_id": 2,
        "password": "Lalala123"
      }
  auth:
    type: bearer
    token: "{{TOKEN}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
