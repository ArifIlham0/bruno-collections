info:
  name: All
  type: http
  seq: 2

http:
  method: POST
  url: "{{BASE_URL}}/teacher/fetch-teachers/"
  auth:
    type: bearer
    token: "{{TOKEN}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
