info:
  name: Create
  type: http
  seq: 1

http:
  method: POST
  url: "{{BASE_URL}}/teacher/"
  body:
    type: multipart-form
    data:
      - name: full_name
        type: text
        value: Teacher Belom Aktif 8
      - name: school
        type: text
        value: SMAN 3 Tambun
      - name: email
        type: text
        value: teachernotactive8@gmail.com
      - name: phone_number
        type: text
        value: "087875686349"
      - name: files
        type: file
        value:
          - /Users/dna/Downloads/banner.png
        disabled: true
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
