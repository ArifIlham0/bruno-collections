info:
  name: Create
  type: http
  seq: 1

http:
  method: POST
  url: "{{BASE_URL}}/teacher/"
  body:
    type: multipart-form
    data:
      - name: full_name
        type: text
        value: Teacher 8
      - name: school
        type: text
        value: SMAN 2 Babelan
      - name: email
        type: text
        value: teacher8@gmail.com
      - name: phone_number
        type: text
        value: "087875686341"
      - name: files
        type: file
        value:
          - /Users/Ilham/Downloads/logo_pkk.png
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
