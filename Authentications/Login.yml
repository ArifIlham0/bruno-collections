info:
  name: Login
  type: http
  seq: 1

http:
  method: POST
  url: "{{BASE_URL}}/authentication/login/"
  body:
    type: json
    data: |+
      {
        "email_or_username": "testmentor@gmail.com",
        "password": "Lalala123"
      }

  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
