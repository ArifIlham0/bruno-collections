info:
  name: Statistic
  type: http
  seq: 1

http:
  method: POST
  url: "{{BASE_URL}}/mentor/fetch-statistic/"
  auth:
    type: bearer
    token: "{{TOKEN}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
